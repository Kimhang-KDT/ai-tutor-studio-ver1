import React from 'react';

const CreateDataset: React.FC = () => {
  return (
    <div className="create-dataset-component">
      <h2>데이터셋 생성</h2>
      {/* 여기에 데이터셋 생성 폼 추가 */}
    </div>
  );
};

export default CreateDataset;
